1.你在製作專題時你遇到什麼困難（請列舉）
    (1).一開始上網GOOGLE找訊息 很多都是2014 2016 2017的資訊，
        因為網站給的是圖片，手動KEY完所有程式碼，程式可以跑，沒有輸出任何東西。
        後續查詢python語法有一些改變，爬蟲用的無頭瀏覽器PhantomJS也已經停止開發，json方式抓資料也是執行程式沒問題但是一樣沒輸出。

    (2).F12開發工具撈到的 Elements 無法全部複製 
        只能一行行複製我要的元素丟給chatGPT，導致GPT只能依照單行給出的程式碼不能直接使用

    (3).使用上課教的範本來改也遇到soup.find ， soup.find_all 的差別搞不懂 改成soup.find_all 程式就出錯
    
2.你如何解決你的困難？
    (1).改成詢問chatGPT，產出的程式存到test.py 來執行一樣沒輸出，但是看得出來裡面程式的class參數要自己改才能讓他跑得起來。
    
    (2).依照上課教的範例4-6-1.py，一一去改，像是啟動瀏覽器chatGPT的不會跳出瀏覽器，所以把上課可以用的那段瀏覽器程式碼ChromeDriverManager先抓出來用，
        然後依照Chrome F12的功能，把商品名稱還有商品價格 "span", class_=指定參數，去把參數丟給chatGPT產出大概語法然後再修改，可以跑出一筆資料。

    (3).因為給chatGPT 只是一行 "span", class_= 的資訊，GPT產出的輸出格式只有if else 只能跑出一次資料 不會把所有的"span", class_=都撈出來，
        然後再依照上課的4-6-1.py的for迴圈去修改後 還是不能使用卡在soup.find soup.find_all 判斷問題，後續也是把前面整段能執行的程式拿去詢問chatGPT，
        然後產出能跑的for迴圈 去把"當前"網頁資料撈出來。

    (4).後續還想把其他參數加進去，還有讓它變成能用的APP，因為平常手機使用會比較方便。    

3.你認為這個專題你自己打幾分（1-5可小數），老師會給你一張小便條，上面請你寫自己的名字和你打的分數（報告結束的當下），今日報告時間從早上9點10分開始。
     3分 一邊做就會一邊想做出更多功能，但是想歸想還是只能一步一步農出功能才行。